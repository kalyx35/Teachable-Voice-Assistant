<?xml version="1.0" encoding="utf-8"?>
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_service_description"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagIncludeNotImportantViews"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100" />

    <!--
        Notes:
        - typeAllMask: receive every event type while we explore. Narrow this later
          (e.g. typeViewClicked|typeViewTextChanged|typeWindowStateChanged|typeViewScrolled).
        - flagReportViewIds: required for AccessibilityNodeInfo.viewIdResourceName.
        - flagIncludeNotImportantViews: include views the app marked as
          "not important for accessibility", so the tree is as complete as possible.
        - canRetrieveWindowContent: required for getRootInActiveWindow() to return a tree.
        - No android:packageNames attribute = the service observes all apps.
        - notificationTimeout: minimum ms between events of the same type.
    -->